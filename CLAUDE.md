# CLAUDE.md

## 项目简介

这是一个 mini Claude Code 的 CLI 工具，用 TypeScript/Node.js 实现，支持 OpenAI 兼容接口。

项目的核心设计目标是**高可扩展性**和**高解耦性**：模块之间通过定义好的接口通信，新增功能优先考虑扩展现有接口，而不是修改已有实现。每个模块可以独立开发和测试。

完整的架构设计、目录结构、模块依赖关系和实现计划详见 `plan.md`。

---

## 代码风格

- **易读优先**：在不影响功能和性能的前提下，代码应该让人容易理解。避免过度简洁或炫技式写法。
- **异步处理**：使用 `async/await`，避免 `.then()` 链。
- **注释**：复杂逻辑必须写注释说明意图，函数/类导出建议写简短的说明注释。

---

## 提示词管理

`src/prompts` 目录用于集中管理提示词文件。满足以下任一条件，请将提示词单独抽取到该目录，而非内联在代码中：

- 提示词较长（超过 5 行或 200 字）
- 后续可能频繁迭代调整

简短且稳定的提示词可以直接内联在代码中。

---

## 测试文件组织

- **位置**：测试文件放在对应模块的目录下，根目录只放全局集成测试。
- **命名**：以 `test-*.ts` 或 `*.test.ts` 命名。
- **示例**：
  - `src/tokens/` 模块的测试 → `src/tokens/test-tokenizer.ts`
  - `src/model/` 模块的测试 → `src/model/test-model.ts`

---

## 接口约定

模块之间通过接口解耦。修改模块时，尽量只改内部实现，保持对外接口稳定。若接口必须变更，需同步更新所有依赖方。

每次开始写一个新模块之前，先讨论并确认该模块对外暴露的接口，写完后补充到同目录下 `types.md` 文件的"模块接口速查"中。